<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:paddingBottom="10dp"
    >
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/estate_dialog_bg"
        >
        <TextView
            android:id="@+id/choose_email_dialog_title_tv"
            android:layout_width="match_parent"
            android:layout_height="@dimen/choose_email_dialog_item_height"
            android:text="Please Choose Email Send Way"
            android:gravity="center"
            />
        <ListView
            android:id="@+id/choose_email_dialog_listview"
            android:layout_below="@id/choose_email_dialog_title_tv"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:listSelector="@android:color/transparent"
            android:divider="@null"
            >
        </ListView>
    </LinearLayout>

</RelativeLayout>